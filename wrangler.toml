name = "cyfsa-ontario-platform"
compatibility_date = "2024-04-01"
compatibility_flags = [ "nodejs_compat" ]
pages_build_output_dir = "dist"

# Environment Variables
[vars]
# ANTHROPIC_API_KEY should be added as an encrypted secret in the deployment environment variables.
# Node compatibility is enabled by default in modern Workers & Pages.
